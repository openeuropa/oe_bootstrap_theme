{#
/**
 * @file
 * Theme override for a progress bar.
 *
 * Note that the core Batch API uses this only for non-JavaScript batch jobs.
 *
 * Available variables:
 * - progress: Percentage of progress in the bar.
 * - min: Minimum state of prorgress bar.
 * - max: Maximum state of prorgress bar.
 * - striped: Striped style of state.
 * - animated: Animation on striped style of state.
 * - label: Label in the bar.
 * - variant: Bar style.
 */
#}

{{ pattern('progress', {
  'progress': progress,
  'min': min,
  'max': max,
  'striped': striped,
  'animated': animated,
  'label': label,
  'variant': variant,
}) }}
