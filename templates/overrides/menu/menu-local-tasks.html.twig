{#
/**
 * @file
 * Template primary and secondary local tasks.
 */
#}
{% if primary %}
  {{ pattern('navigation', {
    'variant': 'tabs',
    'items': primary,
  }) }}
{% endif %}

{% if secondary %}
  {{ pattern('navigation', {
    'variant': 'pills',
    'items': secondary,
  }) }}
{% endif %}
