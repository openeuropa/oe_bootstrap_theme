{#
/**
 * @file
 * Override for breadcrumb trail.
 */
#}
{% if breadcrumb %}
  {{ pattern('breadcrumb', {
    'items': breadcrumb,
    'attributes': {
      'class': [
        'mt-3'
      ],
      'aria-label': "breadcrumb"
    }
  }) }}
{% endif %}
