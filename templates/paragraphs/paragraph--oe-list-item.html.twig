{#
/**
 * @file
 * Theme override to display the 'list item' type paragraph.
 *
 * @see ./modules/contrib/paragraphs/templates/paragraph.html.twig
 */
#}
{{ pattern('list_item', {
  'variant': variant,
  'url': url,
  'title': title,
  'detail': detail,
  'meta': meta,
  'date': date,
  'content': content,
  'image': image,
}) }}
