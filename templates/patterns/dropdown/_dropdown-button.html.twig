{{ pattern('button', {
  label: label,
  style: style,
  outline: outline,
  size: size,
  disabled: disabled,
  attributes: create_attribute({
    'class': 'dropdown-toggle',
    'data-toggle': 'dropdown',
    'data-boundary': 'window',
    'aria-expanded': 'false'
  })
}) }}
