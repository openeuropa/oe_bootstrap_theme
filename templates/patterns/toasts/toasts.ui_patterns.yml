toasts:
  label: "Toasts"
  description: "Push notifications to your visitors with a toast, a lightweight and easily customizable alert message. https://v5.getbootstrap.com/docs/5.0/components/toasts/"
  settings:
    with_wrapper:
      type: boolean
      label: With wrapper?
      preview: true
    with_container:
      type: boolean
      label: With container?
      preview: true
  fields:
    role:
      type: string
      label: "Role"
      description: "Toast role"
      preview: "alert"
    title:
      type: html
      label: "Title"
      description: "Toast title."
      preview: "Bootstrap"
    autohide:
      type: boolean
      label: "Autohide"
      description: "If it will be hidden automatically"
      preview: false
    with_close:
      type: boolean
      label: "With close"
      description: "Close button."
      preview: true
    body:
      type: string
      label: "Body"
      description: "Toast body."
      preview: "See? Just like this."
    time:
      label: "Time"
      type: "DateTime"
      description: "A DateTime object."
      preview: "1639404765"
