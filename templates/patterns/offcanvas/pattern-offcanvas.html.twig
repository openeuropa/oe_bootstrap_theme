{#
/**
 * @file
 * Offcanvas pattern.
 */
#}
{% set _id = attributes.id|default(offcanvas_id) %}
{% if title is not empty %}
  {% set _title = {
    'label': title,
    'heading': 5
  } %}
{% endif %}

{% include '@oe-bcl/offcanvas' with {
  'title': _title,
  'placement': placement,
  'id': _id,
  'body': body,
  'with_body_scroll': with_body_scroll,
  'with_backdrop': with_backdrop,
  'extra_classes_body': extra_classes_body,
  'extra_classes_header': extra_classes_header,
  'extra_classes_close': extra_classes_close,
  'attributes': attributes,
} only %}

{% if toggle is not empty %}
  {% set _toggle_attributes = toggle.attributes|default({}) %}
  {% set _toggle_attributes = _toggle_attributes|merge({
    'data-bs-toggle': 'offcanvas',
    'data-bs-target': '#' ~  _id,
    'aria-controls': _id
  }, true) %}
  {% set _toggle = toggle|merge({
    attributes: _toggle_attributes
  }) %}
  {{ pattern('button', _toggle) }}
{% endif %}
