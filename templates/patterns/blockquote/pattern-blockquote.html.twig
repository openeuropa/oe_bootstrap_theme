{# Parameters:
  - quote (string) (default: '')
  - attribution (string) (default: '')
  - cite (string) (default: '')
  - alignment (string) (default: 'left')
    - options ["left", "center", "end"]
#}

{% include '@oe-bcl/bcl-blockquote/blockquote.html.twig' with {
  'quote': quote,
  'attribution': attribution,
  'cite': cite,
  'direction': direction,
  'alignment': alignment,
} %}