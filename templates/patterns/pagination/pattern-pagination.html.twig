{#
{#
/**
 * @file
 * Pagination pattern.
 */
#}
{% include '@oe-bcl/pagination' with {
  'size': size,
  'alignment': alignment,
  'aria_label': aria_label,
  'enable_icon': enable_icons,
  'prev': prev,
  'next': next,
  'items': items,
  'extra_classes': extra_classes,
  'attributes': attributes,
} only %}
