{#
/**
 * @file
 * Banner component.
 */
#}
{% set attributes = attributes.addClass([
    'bg-light',
    'text-dark',
  ])
%}
{% if centered  %}
  {% set attributes = attributes.addClass(['text-center'])%}
{% endif %}
{% include '@oe-bcl/bcl-banner/banner.html.twig' with {
  'title': title,
  'description': description,
  'link': link,
  'shade': shade,
  'hero': hero,
  'full_width': full_width,
  'image': '',
  'attributes': attributes
} %}

