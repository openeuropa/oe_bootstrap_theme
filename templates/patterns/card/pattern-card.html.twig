{% if equal_height %}
  {% set attributes = attributes.addClass('h-100') %}
{% endif %}

<div{{ attributes.addClass('card') }}>
  {% if image and image_position != 'bottom' %}
    {{ image }}
  {% endif %}
  {{ include('@oe_bootstrap_theme/patterns/card/_card-content.html.twig') }}
  {% if image and image_position == 'bottom' %}
    {{ image }}
  {% endif %}
</div>
