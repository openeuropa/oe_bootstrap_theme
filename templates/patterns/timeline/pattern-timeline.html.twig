{#
/**
 * @file
 * Timeline pattern.
 *
 */
#}
{% set _toggle_collapsed = toggle_collapsed|default('Display %d more'|t) %}
{% set _toggle_expanded = toggle_expanded|default('Stash %d items'|t) %}
{% include '@oe-bcl/bcl-timeline/timeline.html.twig' with {
  toggle_collapsed: _toggle_collapsed,
  toggle_expanded: _toggle_expanded,
  hide: {
    from: hide_from,
    to: hide_to|default(0),
  },
  items: items,
  icons_path: bcl_icon_path,
  attributes: attributes,
} only %}
