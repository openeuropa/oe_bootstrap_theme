<div{{ attributes.addClass('jumbotron bg-light p-5 rounded-lg m-3') }}>
  {% if 'fluid' in variant %}
  <div class="container">
    {% endif %}
    {{ content }}
    {% if 'fluid' in variant %}
  </div>
  {% endif %}
</div>
