name: Icon
description: 'Displays an icon from an SVG sprite.'
props:
  type: object
  properties:
    name:
      title: Name
      type: [string, 'null']
      description: 'Name of the icon in the SVG sprite.'
    size:
      title: Size
      type: [string, 'null']
      description: 'Size of the icon. Defaults to s.'
      enum:
        - 2xs
        - xs
        - s
        - m
        - l
        - xl
        - 2xl
        - fluid
        - null
      'meta:enum':
        2xs: '2XS'
        xs: XS
        s: 'S (default)'
        m: M
        l: L
        xl: XL
        2xl: '2XL'
        fluid: Fluid
        # Value NULL becomes '' when used as array key.
        '': Default
    transformation:
      title: Transformation
      type: [string, 'null']
      description: 'Optional rotation or flip transformation.'
      enum:
        - rotate-90
        - rotate-180
        - rotate-270
        - flip-horizontal
        - flip-vertical
        - null
      'meta:enum':
        rotate-90: 'Rotate 90 degrees'
        rotate-180: 'Rotate 180 degrees'
        rotate-270: 'Rotate 270 degrees'
        flip-horizontal: 'Flip horizontally'
        flip-vertical: 'Flip vertically'
        # Value NULL becomes '' when used as array key.
        '': Default
    icon_path:
      title: 'Icon path'
      type: [string, 'null']
      description: 'Path to the SVG sprite. Defaults to the BCL icon path.'
    attributes:
      title: Attributes
      type: Drupal\Core\Template\Attribute
