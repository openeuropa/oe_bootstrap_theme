name: 'Button'
description: 'For actions in forms, dialogs, and more with support for multiple sizes, states, and icons.'
props:
  type: object
  properties:
    label:
      title: Label
      type: [string, array, object, 'null']
      description: 'The button label.'
    variant:
      title: Variant
      type: [string, 'null']
      description: 'The button variant. Defaults to primary.'
      enum:
        - primary
        - secondary
        - success
        - danger
        - warning
        - info
        - light
        - dark
        - link
        - null
      'meta:enum':
        primary: 'Primary (default)'
        secondary: Secondary
        success: Success
        danger: Danger
        warning: Warning
        info: Info
        light: Light
        dark: Dark
        link: Link
        # Value NULL becomes '' when used as array key.
        '': Default
    size:
      title: Size
      type: [string, 'null']
      description: 'Size of the button. Defaults to md.'
      enum:
        - sm
        - md
        - lg
        - null
      'meta:enum':
        sm: Small
        md: 'Medium (default)'
        lg: Large
        # Value NULL becomes '' when used as array key.
        '': Default
    type:
      title: Type
      type: [string, 'null']
      description: 'Sets the button behavior type. Defaults to button.'
      enum:
        - button
        - submit
        - reset
        - null
      'meta:enum':
        button: 'Button (default)'
        submit: Submit
        reset: Reset
        # Value NULL becomes '' when used as array key.
        '': Default
    outline:
      title: Outline
      type: [boolean, 'null']
      description: 'Lighter button style removing background color. Defaults to false.'
    disabled:
      title: Disabled
      type: [boolean, 'null']
      description: 'If true, the button will not be enabled when clicking. Defaults to false.'
    text_nowrap:
      title: 'No text wrap'
      type: [boolean, 'null']
      description: 'Prevents the button label from wrapping.'
    assistive_text:
      title: 'Assistive text'
      type: [string, 'null']
      description: 'Visually hidden text printed before the button label.'
    id:
      title: ID
      type: [string, 'null']
      description: 'Button ID attribute.'
    icon:
      title: Icon
      description: 'Icon name as a string or an icon object.'
      type: [string, object, 'null']
      properties:
        name:
          type: [string, 'null']
        path:
          type: [string, 'null']
        transformation:
          type: [string, 'null']
        attributes:
          type: [object, 'null']
    icon_position:
      title: 'Icon position'
      type: [string, 'null']
      description: 'If button has an icon, it can be placed before or after text. Defaults to after.'
      enum:
        - before
        - after
        - null
      'meta:enum':
        before: Before
        after: 'After (default)'
        # Value NULL becomes '' when used as array key.
        '': Default
    icon_spacing:
      title: 'Icon spacing'
      type: [string, 'null']
      description: 'Spacing between icon and label.'
      enum:
        - sm
        - md
        - lg
        - null
      'meta:enum':
        sm: Small
        md: Medium
        lg: Large
        # Value NULL becomes '' when used as array key.
        '': Default
    spinner:
      title: Spinner
      type: [object, 'null']
      description: 'Spinner object.'
      properties:
        type:
          type: [string, 'null']
        variant:
          type: [string, 'null']
        size:
          type: [string, 'null']
        assistive_text:
          type: [string, 'null']
        attributes:
          type: [object, 'null']
    show_spinner:
      title: 'Show spinner'
      type: [boolean, 'null']
      description: 'Displays the spinner before the button label.'
    clean_class:
      title: 'Clean class'
      type: [boolean, 'null']
      description: 'If true, BCL button classes are not added.'
    remove_icon_spacers:
      title: 'Remove icon spacers'
      type: [boolean, 'null']
      description: 'Removes the default spacing between icon and label.'
    attributes:
      title: Attributes
      type: Drupal\Core\Template\Attribute
