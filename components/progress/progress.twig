{#
/**
 * @file
 * Progress component.
 */
#}
{% include '@oe-bcl/progress' with {
  progress: progress,
  min: min,
  max: max,
  message: message,
  striped: striped,
  animated: animated,
  hidden_label: hidden_label,
  label: label,
  variant: style,
  bar_label: text,
  attributes: attributes,
} only %}
